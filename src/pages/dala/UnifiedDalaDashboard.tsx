import React from "react";
import DalaDashboard from "./Dashboard";

// ── Main Dala Dashboard Page ─────────────────────────────────────────────────────
const UnifiedDalaDashboard: React.FC = () => {
  return <DalaDashboard />;
};

export default UnifiedDalaDashboard;
